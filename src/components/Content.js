import React from 'react';
import { ContentContainer } from '../styles.js';

const Content = props => {
	return (
		<ContentContainer>
			<p>hi</p>
		</ContentContainer>
	);
};

export default Content;
